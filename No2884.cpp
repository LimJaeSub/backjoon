#include<stdio.h>
/*
int main(void)
{
	int time;
	int minitue;

	scanf_s("%d", &time);
	scanf_s("%d", &minitue);

	if (minitue >= 45)
	{
		minitue = minitue - 45;
	}
	else if (minitue < 45)
	{
		if (time == 0)
		{
			time = 24;
		}
		time = time - 1;
		minitue = 45 - minitue;
		minitue = 60 - minitue;
	}

	printf("%d %d", time, minitue);

}
*/