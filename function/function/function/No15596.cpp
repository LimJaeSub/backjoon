/*#include<stdio.h>
#include<iostream>
#include<vector>

/*long long sum(std::vector<int>& a)
{
	int num;
	int n;
	long long ans = 0;
	std::cout << "how many :";
	std::cin >> n;
	std::cout << std::endl;
	for (int i = 0;i < n;i++)
	{
		std::cout << "input num:";
		std::cin >> num;
		std::cout << std::endl;
		a.push_back(num);
	}
	for (int i = 0;i < n;i++)
	{
		ans = ans+a[i];
	}

	return ans;
	
}
*/

/*
#include <vector>
using namespace std;
long long sum(vector<int>& a) {
	long long ans = 0;
	for (int i =0;i<a.length();i++)
		ans += *iter;
	return ans;
}
*/
